# Day 7 · 第 1 周 · 综合验收：读 CSV → 调 API → 写回文件

> 昨日批改: Day 6 未通过——4 个坑没修、summary.txt 主流程缺失、未提交 git（跑通≠完成，验收标准要逐条自证） | 连续打卡: 5 天

## 今日目标

第 1 周重排计划的收官日。今天结束时你会完成两件事：把 Day 6 的尾巴收干净（修 4 个坑 + 补综合③），然后独立跑通第 1 周综合验收——**读 CSV → 调公开 API → 处理 → 写回文件**。这个「读数据 → 调外部服务 → 处理 → 落盘」的骨架，就是第 5 周 RAG 项目「读文档 → 调 embedding API → 写向量库」的缩小版，也是面试里「数据管道」问题的最小答案。

## 今日知识点预习 (10-15 min)

动手前先过一遍，有概念再写代码：

1. **requests 库**：`requests.get(url, timeout=10)` 发 GET 请求；`.status_code` 是 HTTP 状态码（200 = 成功）；`.json()` 把响应体解析成 Python 的 dict/list
2. **URL 查询参数**：`?latitude=39.9&longitude=116.4` 是问号后面的键值对，API 靠它收参数
3. **timeout 为什么必须设**：网络请求可能永远不返回（断网 / 服务器挂），不设 timeout 程序会无限卡住；文件读取出错会立刻抛异常，网络请求失败可能「沉默地等」——这是今天文件 IO 和网络 IO 最大的区别
4. **嵌套 dict 访问**：`d["current_weather"]["temperature"]` 一层层往下取；取之前先确认 key 存在（`in` 或 `.get()`），不然 KeyError
5. **写 CSV**：手动拼 `"city,temperature,weathercode\n"` 字符串 + `open(..., "w", encoding="utf-8")` 即可，今天不用 csv 模块（第 3 周再上正规工具）
6. **坏数据处理（复习 Day 4/5）**：逐行 try/except，坏行打印原因跳过，绝不整脚本崩溃
7. **单点失败隔离**：每个城市一个 try/except，一个城市失败不影响其他城市

## 任务清单

### 1. 技术英语 (25 min · 对照阅读)

1. 先读**中文** requests 快速上手第 1–3 节（发请求、传参、响应内容）：https://requests.readthedocs.io/zh-cn/latest/user/quickstart.html
2. 再读**英文**原文同一节：https://requests.readthedocs.io/en/latest/user/quickstart.html（中文当拐杖逐句对照）
3. 在 `notes/2026-08-10-integration.md` 写：5 个生词（英文+猜的中文）+ 1 句读不懂的原文 + **3 句中文总结**（上次心得空着，这次必须写满）
4. 从 `notes/technical-english-core.md` 过**第七组** 10 个词（HTTP/API 篇，已为你补进词表）

### 2. 输入 (60-90 min)

- requests 快速上手（上面链接，中文为主）：GET、status_code、`r.json()`、timeout 这四个点够今天用
- 官方教程第 7 章「输入与输出」7.2 读写 JSON（只读前几段，认识 `json.loads` / `json.dumps`）：https://docs.python.org/zh-cn/3/tutorial/inputoutput.html
- 回顾 Day 3 的 dict 嵌套访问 + Day 5 的 `with open` 读写——今天的任务就是把这两块拼起来

### 3. 编码 (180-240 min · 主体)

**Part A（先做，60-90 min）：补 Day 6 尾巴** —— 在 `submissions/2026-08-09/05_oop.py` 里：

1. 修 4 个坑（批改已列明现象，原理都在 Day 6 预习清单里，先自己对着清单想，别急着问 AI）：
   - `__init__` 判空顺序（先判空再赋值，且要赋值给 `self.scores`）
   - `add_score`（想清楚 `append()` 返回什么）
   - `average`（`len()` 返回整数不能 `for i in len(...)`；空列表返回 0.0 不能除零）
   - `__str__`（括号补齐，输出 `张三(90分, 85分) - AI 学习营`）
2. 补综合③：用 `load_scores` 读 `scores.txt` → 生成 Student 列表 → 打印每人平均分 → 写 `summary.txt`（表头 `姓名,平均分`，utf-8）
3. 写 3 条 assert：空列表 average 返回 0.0、add_score 后分数还在、`__str__` 格式正确
4. **运行一次，逐条对照 Day 6 验收标准自证**——这次别看到 exit=0 就停
5. 第一次 commit：`git add submissions/2026-08-09 notes/2026-08-09-oop.md` → `git commit -m "fix: day6 OOP bugs + integration part 3"`（一次 commit 做一件事）

**Part B（主体，120-180 min）：`submissions/2026-08-10/06_integration.py` — 综合验收**

新建 `submissions/2026-08-10/`，完成：

**① 造数据 `cities.csv`**（含表头 `city,latitude,longitude`）：

```
city,latitude,longitude
北京,39.9042,116.4074
上海,31.2304,121.4737
广州,23.1291,113.2644
月球,abc,xyz
```

最后一行是**坏数据**，验证脚本能跳过不崩。

**② 写三个函数**：

- `load_cities(path) -> list[dict]`：读 CSV，坏行打印原因跳过（复用 Day 5 的逐行模式）；文件不存在打印提示、返回空列表
- `fetch_weather(lat, lon) -> dict | None`：GET `https://api.open-meteo.com/v1/forecast?latitude={lat}&longitude={lon}&current_weather=true`（**timeout=10 必须设**），返回 `{"temperature": ..., "weathercode": ...}`；请求失败（超时 / 断网 / 状态码非 200）打印错误、返回 None，不抛异常
- `save_results(rows, path)`：把 `[{"city":..., "temperature":..., "weathercode":...}]` 写进 `weather.csv`（表头 `city,temperature,weathercode`，utf-8）

**③ 主流程**（放文件末尾）：读 cities.csv → 对每个城市 fetch_weather → 跳过返回 None 的城市 → 打印每个城市 `城市 温度` → save_results 写 weather.csv

**④ 验收自测**（写完跑一次，逐条过）：

- 北京/上海/广州三行都出现在 weather.csv，温度与打印一致
- 月球那行被跳过且打印了跳过原因，脚本不崩
- 请求都设了 timeout；单个城市失败不影响其他城市
- 全程自己 debug：可以问 AI「为什么报这个错」，**禁止让 AI 写完整代码**（第 1 周验收的明文规定——AI 代写 = 第 8 周手写 agent 时崩）

> 面试怎么考：这就是「数据管道」的最小形态。面试官问「你做过数据管道吗」，你答的就是这个骨架：读源 → 调服务 → 容错 → 落盘。今天把容错（坏行跳过、超时、单点失败隔离）做扎实，就是你第 13 周能讲的故事。

### 4. 笔记 (30 min)

在 `notes/2026-08-10-integration.md` 追加，回答：

1. 为什么 `requests.get` 必须设 timeout？不设会发生什么？这和 Day 5 读文件有什么本质区别？（提示：文件是本地资源、API 是远程资源——想想「谁可能不响应、能卡多久、你怎么知道它失败了」）
2. `r.json()` 返回的是什么类型？你取 temperature 写了几层下标？把路径写出来。如果 API 改了字段名，你的代码哪里会报错？
3. 今天坏行和失败请求是怎么处理的？如果不处理，`load_cities` 和主循环会发生什么？
4. 复盘 Day 6 的 4 个坑：每个坑用一句话说清「我错在哪、为什么错」（这是你第 13 周面试讲「踩过什么坑」的素材）

## 验收标准

- [ ] Day 6 尾巴：`python 05_oop.py` exit=0，`average()` 空列表返回 0.0、`add_score` 后分数还在、`__str__` 带括号、`summary.txt` 生成且与打印一致（自己先跑一遍验证）
- [ ] `python 06_integration.py` exit=0，无 traceback
- [ ] `weather.csv` 生成，含表头 `city,temperature,weathercode`，内容与打印一致
- [ ] `cities.csv` 的坏行被跳过且有原因打印，脚本不崩
- [ ] 每个请求都设了 timeout；单个城市失败不影响其他城市
- [ ] `notes/2026-08-10-integration.md` 含英语摘录（5 词 + 1 句原文 + 3 句总结）+ 4 个笔记问题
- [ ] 分两次 commit（一次 Day 6 补完，一次 Day 7 验收）并 push

## 提交

```bash
cd "D:/work-coding/Knowledge base/AI_KnowledgeBase"
# 第一次 commit：Day 6 尾巴
git add submissions/2026-08-09 notes/2026-08-09-oop.md
git commit -m "fix: day6 OOP bugs + integration part 3"
# 第二次 commit：Day 7 验收
git add submissions/2026-08-10 notes/2026-08-10-integration.md
git commit -m "feat: day7 week1 integration - csv -> api -> csv"
git push
```

## 今日一问

同样是「从外部拿数据」，为什么 Day 5 读文件不用设 timeout，今天调 API 必须设？这两种失败方式有什么本质不同？（提示：想想「谁可能不响应、要等多久、你怎么知道它失败了」）明天批改会追问。
