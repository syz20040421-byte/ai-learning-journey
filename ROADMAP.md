# AI 应用 → Agent 开发｜13 周求职冲刺路线

> **本文件是唯一事实来源。** 每日任务生成器读这份文件决定教什么。
> 修改这里 = 修改后续所有任务。

## 学员档案

| 项 | 值 |
|---|---|
| 背景 | 计算机专业出身，底子薄（需第 1 周摸底定位） |
| 主语言 | Python（TypeScript 延后，见「语言决策」） |
| 强度 | 三级档位（2026-08-22 起）：最低线 2h（状态差保连续）/ 标准线 4–5h（大多数日子）/ 冲刺线 6–7h（项目集中开发、部署排错日），周一至周五 + 周日，**周六休息**。衡量标准 = 产生了什么可验证结果，不是坐了几小时 |
| 周投入 | 约 12–42 小时（按档位波动） |
| 总投入 | 约 300–450 小时（视档位执行） |
| 目标 | **找工作**（AI 应用开发 / LLM 应用工程师） |
| Deadline | 13 周后投简历 |
| 英语 | 2026-08-17 起停用独立英语学习环节；每日学习时间集中到 Python 工程、AI 应用开发和项目交付 |
| 提交方式 | 本地 `submissions/`（建议尽早迁 GitHub，commit 历史即简历证据） |
| 任务交付 | Obsidian `daily/`（2026-08-09 起**不再推送飞书**，学员直接看本地任务文件） |

> **目录归档约定（2026-08-13 起）**：`daily/`、`notes/`、`submissions/`、`review/` 下的日期文件统一按 `YYYY-MM/YYYY-Www/`（月/ISO 周）两级归档，如 `daily/2026-08/2026-W33/2026-08-13.md`；无日期文件（如 `notes/technical-english-core.md`）留在目录根。

## 求职导向的三条硬约束

这条路线不是「学得全」，是「13 周后有东西给人看」。招聘方实际看三样：

1. **2 个能跑起来、有线上地址的项目** — 一个 RAG 应用，一个 Agent
2. **GitHub 提交记录** — 提交质量和独立贡献比数量重要；连续提交是坚持的佐证，**不是能力证明**（2026-08-22 外部评审修正）
3. **能讲清技术选型的「为什么」** — 面试区分「会抄」和「会做」的唯一手段

因此：**第 5 周就开始滚项目**，不是学完再做。所有「学得全但没产出」的内容一律砍掉或延后。

---

## 13 周总览

| 周 | 主题 | 交付物（验收标准） |
|---|---|---|
| 1 | 摸底 + 补 Python 硬伤 | 诊断报告 + 一个自己 debug 通的脚本 |
| 2 | Python 进阶 + Git + 环境工程 | GitHub 仓库开张，有 5+ 次有意义 commit |
| 3 | HTTP / FastAPI / SQL | 线上可访问的 API + pytest 测试（手机能打开） |
| 4 | LLM 基础 + 裸调 SDK + Prompt 工程 | 命令行流式聊天工具，支持多轮 + 工具调用 |
| 5 | **项目 A 启动**：RAG 骨架 | 能检索能回答的最小闭环 |
| 6 | 项目 A 完成：按失败数据改进（基线先行，组件按需加） | 部署上线，他人可用 |
| 7 | 工程化：Evals + 可观测性 + Docker | 项目 A 有 20+ 条评测集 + Langfuse 监控 |
| 8 | **手写 Agent**（ReAct，零框架） | 裸写 agent 能完成 3 步以上任务 |
| 9 | 项目 B 启动：真实工作流工具设计（MCP 按需） | 可测的 Agent 工作流 + 20 条任务集 |
| 10 | 项目 B：记忆 / 上下文压缩 / 框架 | Agent 能处理长任务不爆窗口 |
| 11 | 项目 B 完成：可靠性 + 成本控制 | 量化验收达标（成功率/终止/注入测试） |
| 12 | 打磨：README / 部署 / 演示视频 | 两个项目达到可展示状态 |
| 13 | 简历 + 面试 + 八股复盘 | 简历定稿，开始投递 |

---

## 阶段详解

### 第 1 周：摸底 + 补 Python 硬伤

**不从「什么是变量」开始。** Day 1 先做摸底测试，按实际漏洞定起点。

**摸底结论（2026-08-05 批改，路线已按此重排）**
- 会：基础语法、OOP 基本用法、try/except、Git 基本操作
- 不会（本周必修）：数据结构选型与复杂度（dict/set 零接触）、文件 IO、异常机制、类属性 vs 实例属性
- 已重排为 8/5–8/10 每日一课：数据结构 → 异常 → 文件IO → OOP/函数 → 综合验收

**摸底范围**：Python 语法 / 数据结构 / 函数与作用域 / OOP / 异常 / 文件 IO / HTTP 概念 / SQL / Git / 命令行

**核心技术点**
- 数据结构选型：list vs dict vs set vs tuple，各自的时间复杂度
- 函数：参数传递、可变默认参数的坑、作用域
- 异常处理：读懂 traceback，`try/except/finally`
- 文件与格式：`pathlib`、JSON、CSV
- 环境：`uv` 管理虚拟环境与依赖
- 代码质量：type hints、`ruff`

**验收**：写一个脚本，读 CSV → 调公开 API → 处理 → 写回文件。**全程自己 debug**，可以问 AI「为什么错」，禁止让 AI 写完整代码。

---

### 第 2 周：Python 进阶 + Git + 工程习惯

**核心技术点**
- 推导式、生成器、迭代器
- 装饰器（理解即可，不必精通）
- `dataclass` / **Pydantic**（后面 FastAPI 和结构化输出都靠它）
- `async` / `await` 入门 ← AI 应用大量使用
- 模块与包组织、`__init__.py`
- 标准库：`os`、`pathlib`、`datetime`、`json`、`logging`、`typing`
- **Git**：branch / merge / 冲突解决 / `.gitignore` / 写人能看懂的 commit message
- GitHub：仓库、README、Actions 初识

**验收**：GitHub 仓库开张，结构规范（`README.md`、`pyproject.toml`、`.gitignore`），5+ 次有意义 commit。`src/`、`tests/`、pytest、CI 未在第 2 周完成——**转第 3 周结合 FastAPI 项目补齐**（2026-08-22 外部评审修正：工程化部分不算完全通过，Python 基础部分通过）。

---

### 第 3 周：HTTP / FastAPI / SQL

目的不是成为 Web 开发者，是**看懂 AI 应用跑在什么之上**。

**核心技术点**
- HTTP：请求/响应结构、方法、状态码、Header、Body；**状态码语义（2026-08-22 修正）**：找不到资源 `raise HTTPException(404)`，创建成功 201（装饰器 `status_code=`），不合法请求 4xx 不 500；**路由顺序表述修正**：`/students/{name}` 不吞 `/students`（查询参数不参与路由匹配），真冲突是固定子路径（`/students/stats`）要声明在 `{name}` 前
- API：REST 设计、JSON、认证（API Key / Bearer）、限流（429）
- **FastAPI**：路由、Pydantic 校验、依赖注入、异步端点、自动文档、HTTPException
- **httpx**（统一网络库，pyproject 已声明；不用 requests）：同步请求 → 后续过渡 `httpx.AsyncClient`
- **pytest** + FastAPI `TestClient`：正式 `tests/` 目录，正常/错误/边界路径，fixture，重复运行互不污染；第 3 周起 web 任务的「内联自测块」逐步让位给独立测试
- SQL：SELECT / JOIN / GROUP BY / 索引概念
- SQLite + SQLModel（或 SQLAlchemy）；repository/service 分层（数据访问与业务逻辑分离）
- **Streamlit** 或 **Gradio**：用 Python 写界面，跳过前端
- 环境变量与 `.env`、密钥管理（`.env` 不进 Git）
- 部署：Railway / Render / Fly.io 任选其一（国内网络环境注意：需可直连或代理可达，见「风险提示」）

**验收**：FastAPI 服务，5+ 接口，SQLite 持久化（重启数据还在），正确的 2xx/4xx/5xx 语义（404/201/422），pytest 12+ 条接口测试（`tests/` 目录、重复运行不互相污染），密钥不进 Git，部署上线，**手机浏览器能访问**。

---

### 第 4 周：LLM 基础 + 裸调 SDK + Prompt 工程

真正的 AI 应用开发从这里开始。

**4.1 基础概念**
- Token、上下文窗口、temperature / top_p
- 定价模型：输入/输出单价、prompt caching
- 模型选择：什么任务用小模型、什么必须大模型
- 多模态基础

**4.2 直接用官方 SDK（禁止先上框架）**
- OpenAI SDK / Anthropic SDK / DeepSeek（OpenAI 兼容）
- 一次 chat completion 的完整生命周期
- 流式输出（streaming）
- 多轮对话与消息历史管理
- 重试、超时、指数退避、成本统计
- **评测集从第一天建（2026-08-22 起）**：10–20 条固定输入组成初始评测集；区分确定性测试（解析/状态转换/工具参数/权限）与概率性评测（回答质量/检索质量/任务成功率）——LLM 输出不确定，不能拿「一次输出看起来不错」当评测

> **为什么不先用框架**：框架替你做掉大量事情，你会不知道哪一步是它做的。裸写两周后再用框架，才知道它省了什么。第 8 周手写 agent 时这一步的价值会兑现。

**4.3 Prompt 工程**
- System prompt 的作用与写法
- Few-shot 示例
- **结构化输出**：JSON Schema + Pydantic 约束
- **Function Calling / Tool Use** ← 通往 Agent 的门
- 思维链、任务拆解
- Prompt 版本管理

**验收**：命令行聊天工具，支持流式输出、多轮历史、至少 2 个工具调用、token 成本统计；超时/429/网络异常有明确处理路径；业务代码不直接散落 SDK 调用（封装统一模型客户端）；10–20 条初始评测集可重复运行。

---

### 第 5–6 周：项目 A — RAG 应用

**边学边做，不是学完再做。**

| 环节 | 技术点 |
|---|---|
| Embedding | 向量原理、相似度计算、模型选择 |
| 文档处理 | PDF / 网页 / Markdown 解析 |
| **分块策略** | 固定长度 / 语义分块 / 重叠窗口 ← **最影响效果的一环** |
| 向量库 | Chroma（本地起步）→ pgvector / Qdrant（生产） |
| 检索 | 先做最简基线（纯向量检索）；BM25 混合检索**按需加**——只有基线失败案例证明关键词召回不足才加 |
| Rerank | **按需加**——基线评测证明初次检索排序不足时再加；每加一个组件都要回答：解决哪类失败 / 指标提高多少 / 延迟成本增加多少 / 是否值得保留 |
| 生成 | 上下文拼装、**引用来源**、防幻觉 |
| 评估 | 检索命中率、答案忠实度 |
| 界面 | Streamlit / Gradio |
| 部署 | 上线，可分享链接 |

**验收**：喂进真实资料（你的笔记 / PDF / 文档），有界面，部署上线，**另一个人能独立用起来且不需要你解释**，答案带来源引用。

> **基线先行原则（2026-08-22 起）**：第 5 周先做最简单、可测量的基线——真实领域资料 30–50 份 + 评测问题 30+ 条 + 带引用来源的检索回答闭环，记录基线指标；第 6 周按失败数据决定加什么组件。**不预设复杂组件一定更好。**

---

### 第 7 周：工程化

**「能跑」和「能用」的分水岭。** 多数人跳过这段，结果东西一上线就崩。这也是面试时最能拉开差距的部分。

**核心技术点**
- **Evals 升级为版本化回归评测**（基础第 4 周已建）：30+ 条版本化评测集、量化指标、回归测试、LLM-as-judge
- 可观测性：**Langfuse**（开源）或 LangSmith — 追踪每次调用的耗时/成本/输入输出
- 性能：`async` 并发、批处理、语义缓存
- Docker：`Dockerfile`、docker compose
- CI/CD：GitHub Actions 跑测试
- **安全**：Prompt Injection、密钥管理、内容审核、输出校验

> **没有评测，改 prompt 只能靠感觉，无法判断变好还是变坏。** 这是业余和专业的真正分界线，也是面试高频追问点。

**验收**：项目 A 补上 20+ 条评测集、Langfuse 监控、Docker 化。改一次 prompt 立刻能看到分数升降。

---

### 第 8 周：手写 Agent（必做，零框架）

**Agent 的工程组成（2026-08-22 修正：记忆是可选能力，不是必备）**：

```
模型与指令 + 工具及其参数约束 + 状态和控制流 + 最大步骤/超时/预算 + 权限与人工审批 + 失败恢复 + 评测与可观察性
```

不神秘。所有框架都是在这几样上做封装。**记忆（Memory）只有在项目确实需要跨会话保存信息时才引入。**

**本周唯一任务**：从零实现一个 ReAct 循环，**不用任何框架**。

```
思考 → 选工具 → 执行 → 观察结果 → 再思考 → ... → 输出答案
```

做完这一步，Agent 对你就没有神秘感了。这是整个后半程最关键的练习，也是面试时讲得出深度的底气。

**验收**：裸写的 agent 能自主完成 3 步以上的任务，且：工具输入用结构化模型、最大步骤限制、单任务成本上限、工具异常反馈给模型、明确终止状态、危险操作人工确认、状态日志可追踪、工具连续报错不会无限循环。同一任务能复现完整轨迹。

---

### 第 9 周：项目 B 启动 — 工具设计 + MCP

**9.1 工具设计**
- 什么该做成工具、什么不该
- 工具描述怎么写（模型靠描述决定调不调）
- 参数校验、并行工具调用
- **错误如何反馈给模型**让它自己纠正
- 工具太多时的检索与分层

**9.2 MCP（Model Context Protocol）——按需，不是项目目标（2026-08-22 修正）**

MCP 是工具接入方式之一。**只有存在「跨客户端复用工具」的需求时才把工具做成 MCP Server**；验收重点不是写了几个工具，而是：工具是否解决真实任务、schema 是否清晰、权限是否最小化、错误能否被模型理解、危险操作是否要求确认。

**项目 B 定位**：围绕真实业务流程设计 Agent（有明确输入、多步骤、失败结果可判断、有明确用户、能量化节约时间或提高成功率），不为了展示技术堆工具。避免「万能个人助理」。

**验收**：能完成 20 条以上多步骤测试任务的单 Agent 工作流（MCP 按需引入）。

---

### 第 10 周：记忆 / 上下文 / 框架

**10.1 记忆与上下文**
- 短期记忆（对话历史）vs 长期记忆（向量库 / 结构化存储）
- **上下文压缩**：长任务如何不爆窗口
- 上下文工程：什么该进上下文、什么不该
- 状态持久化与断点恢复

**10.2 编排与控制**
- 规划（Planning）与任务分解
- 多智能体：Supervisor 模式、并行子 agent
- **Human-in-the-loop**：审批点、中断与恢复
- 循环终止条件、防止无限循环烧钱

**10.3 选一个框架深入（只选一个）**

| 框架 | 特点 |
|---|---|
| **LangGraph** | 图式编排、状态机、生态最大、招聘提及最多 |
| **PydanticAI** | 类型安全、代码干净、Python 味最正 |
| OpenAI Agents SDK | 轻量、官方、上手快 |
| Claude Agent SDK | 长任务与工具使用能力强 |

**选型原则（2026-08-22 修正）**：不要因为路线写了某个框架就用它。需要明确状态图/暂停恢复 → 图式工作流框架（如 LangGraph）；要类型安全和轻量封装 → 类型友好轻量框架（如 PydanticAI）；项目简单 → 保留手写循环更合适。**定期抽样目标岗位 JD 再定，只深入一个，概念通了换框架成本极低。多 Agent 不是必修项**——只有任务确实能并行、角色边界清楚、单 Agent 已被证明不足时才引入。

---

### 第 11 周：项目 B 完成 — 可靠性

- 沙箱与权限：代码执行沙箱（Docker / E2B）、文件与网络边界、危险操作确认
- Agent 专用评测：多步任务成功率、轨迹评估
- 失败模式分析、成本与延迟优化
- 越权与注入防护

**验收（量化指标，2026-08-22 修正——「连续跑 7 天」降为观察项）**：

| 指标 | 最低要求 |
|---|---|
| 固定任务集规模 | 50 条以上 |
| 多步骤任务成功率 | 80% 以上，报告样本量/置信范围 |
| 无限循环 | 0 次 |
| 超预算后继续调用 | 0 次 |
| 危险操作未经确认执行 | 0 次 |
| 工具异常可恢复率 | 记录并设定项目目标 |
| Prompt Injection 测试 | 有独立测试集和结果 |
| 延迟 / 成本 | 记录 P50/P95 延迟、单任务平均与高分位成本 |

必须**主动注入失败**（工具超时、API 429、返回格式错误、工具返回空结果、模型重复调用同一工具、网络中断、恶意网页/文档指令、用户请求越权操作）验证系统在异常条件下仍可控。可靠性不是「正常跑几次没问题」，而是异常条件下仍然可控。

---

### 第 12 周：打磨

- 两个项目的 README：问题背景、架构图、技术选型理由、部署方式、效果演示、评测方法、基线与改进结果、失败案例、成本/延迟/安全边界
- **真实用户试用（2026-08-22 起）**：至少 3 名真实用户，记录「无需提示能否独立完成任务」，收集失败任务而不只收集好评，修复最常见的三个失败类别
- 部署稳定化，线上地址长期可访问
- 录 2–3 分钟演示视频
- 代码清理：删注释掉的死代码、补类型注解、过 ruff
- 架构图（可用 Excalidraw 风格手绘）

---

### 第 13 周：求职

- 简历：项目描述用「问题 → 方案 → 量化结果」结构，不要写「熟悉 XXX」
- GitHub 主页整理，pin 两个项目
- 面试准备：
  - **必答**：RAG 为什么分块、混合检索为什么比单一好、Agent 循环怎么防死循环、Prompt Injection 怎么防、Evals 怎么设计
  - Python 基础八股、HTTP、SQL、并发
  - 项目深挖：每个技术决策都要能说出「为什么选它，不选另一个」
- 开始投递

---

## 每日时间分配模板（标准线 4–5 小时基准；冲刺线多出的时间全部加在写代码上）

| 时长 | 内容 |
|---|---|
| 20–30 min | **复盘与调试记录**：回顾昨日批改、记录今日卡点，补充可复现的验证实验 |
| 60–90 min | 新知识输入（视频 / 文档 / 书） |
| 180–240 min | **动手写代码**（主体，不可压缩） |
| 30 min | 笔记整理，写进 `notes/` |
| 15 min | 提交作业到 `submissions/`，git commit |

8 小时状态好时，多出的 2 小时全部加在**写代码**上，不加在看教程上。

---

## 验收机制（三层，防水）

**日检 — 代码硬验证**
作业放 `submissions/`，我实际执行。跑不通 = 未完成。我给报错分析，不给答案。

**周检 — 代码复习 + 概念抽查**
每周一天出 3 道左右**代码题**（覆盖本周知识点、带自测，次日实跑批改），文字概念题放周检 `notes.md`（2026-08-19 学员规定）。专抓「代码能跑但不懂原理」。

**阶段检 — 项目验收**
按验收标准逐条过，不过打回重做，不放水。

**反抄袭**
提交里出现超出当前进度的写法（刚学循环就交装饰器 + async），直接指出。不是道德问题 —— 跳过必须经历的痛苦，第 8 周手写 agent 时一定崩。

## 难度自适应

| 情况 | 调整 |
|---|---|
| 连续 2 天未交 | 降难度，任务砍半，先恢复节奏 |
| 连续 5 天轻松完成 | 加码，或提前进下一阶段 |
| 某知识点反复错 | 插入专项复习日 |
| 落后超过 1 周 | 触发路线重排，砍非核心内容保项目 |

---

## 语言决策：为什么 Python 优先

**结论：主语言 Python，一路走到 Agent 开发。**

1. **生态深度不对等** — 模型厂商 SDK 都是 Python 优先；Agent 框架主力（LangGraph、PydanticAI、LlamaIndex、DSPy）在 Python 里发育最完整，TS 版常滞后或功能不全。越往深走（自定义检索、评测、微调、数据处理），Python 优势越大。
2. **求职面** — 国内 AI 应用开发岗位 JD 绝大多数写 Python。
3. **资料质量与数量差一个数量级**，中文资料尤其明显。
4. **排查问题时** Google 到的答案、AI 给的示例代码，默认都是 Python。

**TypeScript 什么时候更好**：要独立做面向用户的 Web 产品时（前后端一套语言、Vercel AI SDK 做流式界面舒服、MCP 的 TS SDK 是一等公民）。但这些优势在「交付产品」层面,不在「理解 AI」层面。

**本路线的处理**：第 12 周若时间有余，花 3–5 天补 TS + Next.js 基础用于项目 A 的界面升级。**不在前 11 周碰 TS** —— 同时学两套语法体系，最常见结果是两个都半生不熟。

---

## 四条原则

1. **项目驱动，别刷教程。** 里程碑比看完课程重要得多。教程看完的成就感是假的，东西做出来的才是真的。
2. **先手写再用框架。** 框架是省重复劳动的,不是省理解的。
3. **工程实践是硬门槛。** 能把 AI 应用跑通、测通、解释清楚，比堆教程和概念更重要。
4. **别追热点。** 框架三个月换一轮，底层的东西（HTTP、Python、评测思维）十年不变。

## 常见陷阱

| 陷阱 | 后果 |
|---|---|
| 跳过 Python 基础直接学框架 | 只会抄示例，一改就崩 |
| 一上来就用 LangChain | 不知道哪步是框架做的,问题定位不了 |
| 不建评测集 | 改 prompt 靠感觉,无法判断好坏 |
| 只做 demo 不做工程化 | 自己能跑,给别人用就崩 |
| 没基线就上混合检索/Rerank 等复杂组件 | 不知道每加一个组件值不值，改 prompt 靠感觉 |
| 同时学 Python 和 TS | 两个都半生不熟 |
| 追新框架 | 学完就过时,基础还是空的 |
| 一直看教程不动手 | 三个月后什么都写不出来 |
| Agent 不设成本上限和终止条件 | 无限循环烧钱 |
| 简历写「熟悉 XXX」 | 面试一问就穿,不如写项目和量化结果 |

---

## 风险提示（实话）

3 个月从底子薄到拿到 AI 应用开发工作，**紧但可能**，前提是真能按档位保持节奏不断线。

风险点不在智力，在**中途掉节奏**。这也是每日打卡 + 硬验证存在的原因。掉了会直接说，不会哄。

另：具体工具和框架名变化很快，本文提到的库到你学到那一步可能已换代。**语言层面和方法论层面的判断是稳定的**，具体选型以官方文档为准，不要信半年前的教程。
