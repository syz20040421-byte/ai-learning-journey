# 学习笔记
（学员自填：编码/技术知识；今天学到什么、卡在哪、哪行代码写完不确定为什么对、自己的疑问）

# 今日三问
1. **（昨天代码追问）** 你 Day 12 的 `compare_times` 用固定 `delay=0.15` 测出并发比串行快约 3 倍，断言写成 `t["concurrent"] < t["sequential"]`。如果把 `asyncio.sleep` 换成真实的网络请求（第 3 周就会），这个断言还会稳定成立吗？真实网络下「并发一定比串行快」还成立吗？要让它不 flaky，你会怎么改这个测试？
2. **（今日机制）** Pydantic 默认宽松：`age: int` 收到字符串 `"21"` 会静默转成 21。这个「宽容」在真实 API 里什么时候会变成坑？举一个具体场景（提示：带前导零的字符串 / 科学计数法 / 布尔值）。
3. **（场景题）** 接口收到 1000 条成绩记录、其中 3 条坏数据。用 `safe_score` 逐条 try/except（坏行跳过、好行入库）vs 用 `load_class` 整体校验（一条坏全部拒收）——各适合什么产品场景？如果产品要求「坏行跳过、好行照常入库」，你选哪种实现？第 3 周 FastAPI 的请求体校验默认是哪种行为？
