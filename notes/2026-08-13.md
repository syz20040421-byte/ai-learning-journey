# 英语
1. 词汇：finger：n.手指，hurt：v.疼痛，whole：adj.完整的，piece：n.乐曲，slowly：adv.慢慢地，mistake：n.错误，start：v.开始，already：adv.已经，beautifully：adv.优美地，smoothly：adv.流畅地，mistake：n.错误，slow：adj.慢的，rough：adj.粗糙的，later：adv.后来，without：prep.没有
2. 摘抄
    - 原文：`She played the same five notes again and again`
    - 译文：她把五个相同的音符练了一遍又一遍（note我不知道除了笔记还有音符的意思）
    - 原文：`I practiced a little more. Every day, I became a little better.`
    - 译文：每天我练习多一点，我就会变得更好一点（单纯喜欢）
    - 原文：`But if you come back after one year, it is taller than your head.`
    - 译文：但如果你一年后再回来，它已经比你的头还高了。（后面那句我给翻译成了比你印象中的高了，把头想成脑海了）
    - 原文：`Progress is quiet. You cannot see it today. But if you look back after one month, you will be surprised how far you have come.`
    - 译文：进步是无声的，你可能今天看不到他，但是一个月之后再回头看，你会为你走了很远而感到骄傲（单纯喜欢）
3. 感悟：
    - 中文：进步是每天一点点累积的，短时间我们很难看到自身的变化。但是长此以往，我们会发现自己已经走了很长一段路，这就是成长的意义。
    - English：Progress is appended a little more every day.In a short time,we cannot see changes in itself. However, if this continues we can find always walk a long road who itself.
    - 语病修改：Progress accumulates little by little every day. In a short period, we can hardly see changes in ourselves. But over time, we will find that we have come a long way. That is the meaning of growth.
    - 存在的语病（ai指出的）：首先，用词不当——将“累积”译作 appended 完全不合适，应改为 accumulated 或 builds up；而 in itself 错误地指代“进步本身”，实际应指“我们自身的变化”，故应为 in ourselves；最后的 who itself 更是词不达意，既无法指代前文，也混淆了人与物的关系。其次，语法结构严重错误——find always walk 缺少宾语从句或补语成分，应改为 find that we have walked 等正确结构。再者，逻辑不完整，中文原句末尾“这就是成长的意义”这一层意思完全没有译出来。最后，搭配不地道，walk a long road 不如英语固定习语 come a long way 自然贴切。整体而言，这些语病导致句意含混、表达生硬。
# 学习笔记
1. 
# 今日三问
1. **（昨天代码追问）** 你 `fetch_weather` 里写的是 `raise requests.RequestException` 再被自己的 `except` 接住。这个 raise 是必要的吗？直接 `return None` 有什么不一样？哪种更直接、更好读？
- raise再except是多余的，直接return None更清晰高效，因为非200状态码属于预期内的业务失败，不该用异常来绕弯子。
- 流程那层多余的 try/except什么都没接住（fetch_weather 内部把异常全接完、只返回 None 永不抛，外层 except 永远触发不了）
2. **（今日机制）** 为什么 `assert ... == "36.8"` 是坏的断言？如果 open-meteo 把字段名 `current_weather` 改成了 `weather`，你的代码哪一行会报什么错？（提示：KeyError 还是 TypeError？发生在哪？）
- 因为天气温度是实时变化的，不可能一直为一个固定的值
- `return_dict["temperature"] = date["current_weather"]["temperature"]` `return_dict["weathercode"] = date["current_weather"]["weathercode"]` 这两行会报错。
- KeyErrorr，返回的json文件中没有`weather`这个键值
- 你这个题的第一问不是问了吗
3. **（场景题）** 你给 weathercode 做了映射表。如果 API 返回一个你表里没有的 code（比如 99），用 `WEATHER.get(code, 默认值)` 和用 `WEATHER[code]`，程序分别会怎样？哪种能保证不崩、还能让用户看懂？
- 会返回默认值
- `WEATHER.get(code, 默认值)`不会崩，返回默认值也能让用户知道哪里出现了问题
- `WEATHER[code]`程序会崩