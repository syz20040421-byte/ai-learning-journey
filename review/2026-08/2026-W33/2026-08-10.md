# Day 6 批改 · 2026-08-10（补批 8/9 提交）

## 结论：未通过（约 60% 完成）

提交存在（`submissions/2026-08/2026-W32/2026-08-09/`），但主体交付缺失、4 个坑一个没修、且**未 commit**。

## 实跑结果

`python 05_oop.py` → exit=0，输出实验②的 6 行打印。**但这是因为主流程没写，类方法根本没被调用**——exit=0 只证明实验段能跑。

用测试输入直接调类方法（实测）：

| 测试 | 结果 |
|---|---|
| `Student("测试").average()` | `TypeError: object of type 'NoneType' has no len()`。`__init__` 判空顺序反了：`self.scores = scores` 在判空之前，且判空赋值给了局部变量 `scores` 而不是 `self.scores`，传 None 时 `self.scores` 仍是 None |
| `s = Student("甲", [90]); s.add_score(85)` | `s.scores` 变成 `None` —— `append()` 返回 None，`self.scores = self.scores.append(score)` 把分数覆盖丢了 |
| `str(Student("张三", [90, 85]))` | `'张三90分, 85分 - AI 学习营'`，缺括号。要求 `张三(90分, 85分) - AI 学习营` |
| `average()` 空列表 | 先 `for i in len(self.scores)` 就 TypeError（`len()` 返回整数，不能迭代）；就算改对，空列表会除零 |

这 4 个坑和 8/10 02:18 落盘的快照里列的一模一样（快照在你最后修改后 8 分钟才提交，你大概率没看到——但跑一次 `average()` 自己就能发现，不用等批改）。

## 完成的部分（要肯定）

- 实验② 完整正确：`AI 学习营 → AI 学院 → 隔壁班` 输出符合预期，3 条 assert 全过，注释解释了每步
- `parse_score_line` / `load_scores` 实现完整，且按 Day 5 批改意见把 `readlines()` 改成了逐行迭代，**Day 5 和 Day 6 两个文件都同步改了**——这个执行力是对的
- `scores.txt` 数据覆盖正确：4 有效 + 1 空行 + 2 坏行
- 笔记：今日一问答对了（类属性存在类命名空间，实例赋值是新增实例属性遮蔽类属性）；英语摘抄按要求做了

## 对照验收标准逐条

- [x] `python 05_oop.py` exit=0（含金量低，见上）
- [x] 实验 3 条 assert + 注释
- [x] 笔记含英语摘录
- [ ] `__init__` 用 scores=None + 判空 → **未过**（判空顺序反）
- [ ] `average()` 空列表返回 0.0 → **未过**（TypeError / 除零）
- [ ] `summary.txt` 生成、含表头、与打印一致 → **未写**，综合③ 主流程整体缺失
- [ ] 笔记 3 问全答 → **未过**（问题 3「卡点」空着；「心得 3 句总结」空着；问题 2 后果答错：默认列表只有一份，两个实例**共享**它，stu1 加分 stu2 的 scores 里也会出现，不是「出现两个成绩」）
- [ ] git add + commit + push → **未做**（工作区还有未提交改动）

## 核心问题：跑通 ≠ 完成

02:05 你跑过一次代码，看到 exit=0 就停了。但 exit=0 只证明「执行到末尾没抛异常」，不证明「每个函数都对」。验收标准是要**逐条自证**的：空列表 average、add_score 后分数还在、`__str__` 格式、summary.txt 生成——这些不写调用代码就永远验证不到。

职业习惯：写完一个函数，立刻用 3 行最小调用把它跑一遍，再写下一个。今天所有验收项都按这个方法来。

## 修法提示（不给答案）

4 个坑的原理全在 Day 6 任务的「预习清单」里（第 9 条可变默认参数；add_score 想清楚 `append()` 返回什么；average 想清楚 `len()` 返回什么）。对着预习清单就能改对，不需要问 AI。

## 处置

Day 6 记为未通过，不记缺勤（你交了东西、也跑了代码，是没做完不是没来）。今天（Day 7）第一段先补 Day 6 尾巴，过了再上综合验收。完成 Day 6 后再把 streak 续上。

---

## 复核（2026-08-10 白天 · 补交版）

学员白天补完并实跑,重新核验:

| 项 | 结果 |
|---|---|
| 4 个坑 | 全修:`__init__` 判空先于赋值且赋给 `self.scores` ✓;`add_score` 直接 `append` 不再覆盖 ✓;`average` 空列表返回 0.0、用 `range` 迭代 ✓;`__str__` 括号齐全 ✓ |
| 方案 A | `load_scores` 返回 `dict[str, list[int]]` 同名追加,平均分真算:张三 99.0=(100+98)/2 ✓ 里斯 93.0 ✓ 英超 70.5 ✓ 法甲 72.0 ✓ |
| 综合③ | 读→对象→打印→写 summary.txt 全通,表头 `姓名,平均分`,内容与打印一致 ✓ |
| 数据 | 8 有效 + 1 空 + 2 坏行,坏行打印原因 ✓ |
| 旧代码 | Day 5 `readlines()` → 逐行迭代,两处同步 ✓ |
| 笔记 | 今日一问正确;英语摘抄 ✓;**「3 句心得」和「问题 3 卡点」仍空缺** |

**复核结论:Day 6 核心达标,记为通过**。唯一尾巴是笔记两处空缺,今天 Day 7 的笔记时间补齐(心得 3 句 + 卡点复盘——正好对应 Day 7 笔记问题 4)。

小提醒(不拦):
- `add_score` 签名是 `-> None` 却 `return self.scores`——要么改签名 `-> list[int]`,要么不 return,二选一保持诚实
- `average` 里负数返回 None 是额外防御,可以;但注意签名已是 `float | None`,写 summary 时若遇负数会写 None——你的数据没负数,暂不触发

根目录有个游离 `summary.txt`(仓库根跑旧版留下的),别 `git add .`,删掉或忽略。

---

## 笔记复核（2026-08-10 晚 · 补齐后）

| 项 | 结果 |
|---|---|
| 心得 3 句 | ✅ 已补:类即蓝图、命名空间/作用域查找顺序、实例优先 + self 自动传参——内容正确,不是凑字数 |
| 问题 3 卡点 | ✅ 已补:「同名多成绩转列表累加」描述准确,对应方案 A |
| 问题 2 后果句 | ⚠️ 仍不准:「这两个学生再调用scores时会出现两个成绩」——共享列表的后果不是「出现两个成绩」,而是**一个学生 add_score,另一个学生的 scores 也会看到新分数**(同一份列表)。首句「scores 都指向这个列表」已对,改最后一句即可 |

**待办(学员)**:改问题 2 最后一句 + `git add submissions/2026-08/2026-W32/2026-08-09 notes/2026-08/2026-W32/2026-08-09-oop.md submissions/2026-08/2026-W32/2026-08-07/04_file_io.py` → commit → push。04_file_io.py 的 readlines 重构可同 commit 或单独 commit。
