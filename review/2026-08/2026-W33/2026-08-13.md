# Day 8 编码部分提前批改 · 2026-08-13（批 d40f07e）

## 结论：编码部分通过 ✅（学员 8/12 16:16 提前完成，已 push）

Day 8 任务书的第 3 项「编码收尾」学员在 8/12 下午就做完了（`c4ba143` 提交主体、`d40f07e` 修尾巴）。本日实跑复核：

```
.venv/Scripts/python.exe submissions/2026-08/2026-W33/2026-08-10/06_integration.py → exit=0
北京，25.6℃ ，晴 / 上海，28.5℃ ，阴 / 广州，31.6℃ ，多云（真实 open-meteo）
```

## 逐条对照（任务书验收标准）

| 验收项 | 结果 |
|---|---|
| docstring 有「昨天没写完卡在」一行 | ✅（写「昨天没空写」，指 8/11 个人原因，属实） |
| load_cities 纬度坏也 `continue` | ✅（经纬转换同 try，except 后 continue） |
| 错误信息不再说 temperature/weathercode | ✅（改为「{城市}的数据异常」；没写「纬度/经度」但已不误导，达标，可更精确） |
| 断言改类型/范围、不含实时值 | ✅（-50<t<60、0<=w<=99 范围断言，36.8 已删） |
| 输出带天气描述、未知 code 不崩 | ✅（WEATHER.get(code, 默认值) 兜底） |
| 重跑 exit=0 | ✅ |

## 新增观察（三问素材，明天追问用）

1. **主流程死代码**：`try/except requests.RequestException` 包着 `fetch_weather(...)`——但 fetch_weather 内部自己 except 并返回 None，**永远不会抛异常**，这层 try 接不到任何东西。三问第 1 题考的就是这个点（raise 再自己接住是绕弯）。
2. **raise 丢了错误原因**：`raise requests.RequestException` 是裸类、无消息 → except 里 `print(f"发生错误：{e}")` 打出来是空的。直接 `return None` 更直接；要 raise 就该带消息。

## 今日剩余（学员待办）

- [ ] 英语阅读 → `notes/2026-08/2026-W33/2026-08-13.md`
- [ ] 自由笔记 + 三问
- [ ] commit + push（代码 + 笔记一起）

---

## 笔记检查（8/13 午后，学员提交初稿后）

**英语模块：达标且超额** —— 词汇 15 个（要求 5-8，但 `mistake` 重复出现两次，下次检查一遍）；摘抄 4 句（要求 1-2）且每句带译文和自纠（「note 不知道还有音符的意思」「把头想成脑海了」——这种诚实标注是学英语最该有的状态）；感悟中英双语。英文原稿语法有错，但规则就是「写出来就是赢」。学员自己写完后用 AI 纠错并把语病分析贴进笔记——允许（先写后对照是学习闭环），但**追问**：能用自己话解释 `come a long way` / `appended` 为什么错？答得出才算真吸收，答不出说明是粘贴不是理解。

**今日三问：Q2、Q3 对，Q1 错（必须重答）**

- **Q1 ❌**：「raise 是必要的，只有这样才可以打印错误信息」——不对。错误信息在 except 里 print 就行，不需要 raise。而且你自己的代码正好证明了 raise 是坏的：`raise requests.RequestException` 是裸异常（无消息），`print(f"发生错误：{e}")` 打出来是**空的**——真正的错误（超时/连接失败）消息在 `requests.get` 抛的原始异常里，被你的 raise 换成了空壳。正确做法：except 里直接 print + return None；或想交给调用者就 raise 带消息、自己不接。主流程那层 `try/except requests.RequestException` 是死代码（fetch_weather 永远不抛、只返回 None）。**要求：notes 里 Q1 答案重写。**
- **Q2 ✅** 对（KeyError，在 `date["current_weather"]` 那两行；注意是 `current_weather` 这个键不存在，不是「没有 weather」）。学员指出第一问重复——**成立**，前半问在尾巴#2/预习已覆盖，后半问才是新考点，以后这类题只出后半。笔误 `KeyErrorr`。
- **Q3 ✅** 对（`get` 返回默认值不崩；`[code]` 抛 KeyError 崩）。小补充：`get` 不传默认值返回 None。

**学习笔记空着**：按学员约定（只记编码/技术知识）今天无编码可不写；若看了输入（HTTP/csv），`status_code`/`r.json()`/`DictReader` 算技术知识，可补，不强求。

**待办**：notes 未 commit；Q1 重答后一并提交。

---

## 重答复核（8/13 晚，学员重写 Q1 答案 + 重写 fetch_weather 后）

**Q1 重答：✅ 通过，附两点补充**

新答案「raise 再 except 是多余的，直接 return None 更清晰高效，非 200 是预期内的业务失败，不该用异常绕弯子」——核心点对了（手动 raise 再自己接住是绕弯）。但：

1. **只答了一半**：任务书提示点名的主流程那层 `try/except` 没答。那层是死代码——fetch_weather 内部把异常全接完、只返回 None 永不抛，外层 except 永远触发不了；代码也没删（`06_integration.py` 105-109 行）。**明日追问：主流程那层 try/except 接得住什么？删掉它重跑，行为有变化吗？**
2. **答案和代码打架**：答案说「直接 return None 更清晰」，代码却用了 `resp.raise_for_status()`（非 2xx 照样抛）。两种写法都成立，但要说得出 raise_for_status 为什么也行——它抛的是带真实信息的 HTTPError，不是之前那种空壳裸 raise。已实跑验证：`fetch_weather(999, 999)` → 打印「请求异常: 400 Client Error: Bad Request for url: ...」→ 返回 None，错误信息不再是空的。

**代码复查（fetch_weather 重写）**：

- ✅ 裸 raise 删除；`raise_for_status()` 代替手写 status_code 判断（更地道）
- ✅ 异常三类分开处理（RequestException / 解析结构 / 兜底），全部 print 带信息 + return None，符合「不抛异常」要求
- ✅ 实跑 exit=0：北京 27.9℃ 阴 / 上海 28.7℃ 毛毛雨 / 广州 35.0℃ 阴；自测断言全过
- ⚠️ 小坑 1（可不改）：`requests.exceptions.JSONDecodeError` 是 RequestException 的子类（已实跑验证 MRO），`resp.json()` 解析失败会被第一个 except 接住、打成「请求异常」——标签不准但不影响功能；想让标签准就把 json 解析拆出 try 或调整 except 顺序
- ⚠️ 小坑 2（可不改）：`except Exception` 兜底会把编程错误（变量名拼错等）也吞成「未知异常」+ None，调试线索少一层；现在能打印所以够用，以后项目里慎用裸兜底
- ❌ 未动：主流程死 try/except（105-109 行）仍在——建议删掉，或留作明日追问素材

**英语模块**：无新改动，维持上次判定（达标超额；`mistake` 重复；surprised 译「骄傲」可顺带纠）。

**学习笔记**：仍空，符合约定（今日无编码任务）。

**文件状态**：notes/2026-08/2026-W33/2026-08-13.md、代码改动、weather.csv（随重跑更新 27.9/28.7/35.0）均未提交——学员一并 commit + push 后 Day 8 收官。

---

## 周 1 巩固三题批改（week1-review · 8/13 晚提交 94e7d08 · 8/14 复跑）

**结论：q1/q2/q3 全部通过 ✅（8/14 用仓库 .venv 逐文件实跑，三份均 exit=0，输出与任务书预期完全一致）**

| 题 | 内容 | 判定 | 亮点 / 问题 |
|---|---|---|---|
| q1 | 链路追踪：CSV → 假 API → None 流向 | ✅ | `load_cities` 按要求换用 `csv.DictReader` 逐行（不再手拆字符串），坏行（月球 abc）打印原因并 continue，文件不存在返回 `[]`；`process_cities` 的 `if date1 is not None` 正是 Day 7 主循环那行的复刻，过滤正确 |
| q1 思考题 | unsafe 崩在哪一行、把 None 当什么用 | ✅ | 答「试图用它去更新另一个字典，而它不是一个有效数据结构」——对。**有意思的点**：任务书预判崩在 `data["temperature"]`（subscriptable），你的实现实际崩在 `date.update(date1)`（`'NoneType' object is not iterable`）——崩点不同说明你是自己写的版本，不是抄的，好 |
| q2 | dict O(1) vs list O(n) 实测 | ✅ | 10 万条记录查 1000 次：dict 0.0001s vs list 4.14s，**慢 42931 倍**——O(1)/O(n) 从概念变成了眼睛看得见的差距；查不到返回 `[]` 而非 KeyError，调用方免判空，习惯好 |
| q2 思考题 | 同名「张三」dict/list 怎么处理、怎么分清 | ✅ | 三连答全对：dict 合并到同一人、tuple 版分不清谁是谁、加学号（唯一键）。「加学号」就是数据库主键思想的雏形，第 3 周学 SQL 时你会再见它 |
| q3 | 读软失败 vs 写硬失败 | ✅ | `save_results_checked` 成功 True / 失败打印具体错误返回 False，`except OSError` 一族接住（FileNotFoundError⊂OSError 用对了）；写完能原样读回；测试产物 os.remove 清理，干净 |
| q3 思考题 | 读失败为什么可以软、写失败不行 | ✅ | 「读失败不会使数据发生变化，写失败会，而且还会影响文件状态」——方向对。补充完整版：写一半崩了文件里剩半截，下游读到半截数据会当完整数据用，错得悄无声息 |

**新观察（不是问题）**：q1 unsafe 版的崩溃点与任务书提示不同，但思考题答得准确——这说明你不是对着提示凑答案，而是真读了自己的代码。继续保持这种「自己的代码自己说得清」。

**遗留（→ 已转 Day 9 任务）**：
1. **归档回归 bug（8/14 教练复跑发现）**：`06_integration.py` 内部的硬编码路径（`submissions/2026-08-10/cities.csv`）在 8/13 目录归档（`7cb2c8d`，移到 `2026-08/2026-W33/2026-08-10/`）后全部失效，从仓库根目录运行直接 FileNotFoundError。这不是你的代码错，是教练归档时没同步脚本里的路径——但它正好暴露了「硬编码相对路径」的脆弱性，Day 9 的任务就是改用 `pathlib` 让脚本任何目录都能跑。
2. **主流程死 try/except（105-109 行）仍未删**——已排进 Day 9 三问第 1 题，动手删了再答。
