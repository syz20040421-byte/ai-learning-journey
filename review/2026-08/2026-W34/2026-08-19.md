# Week 2 周检批改（补交）· 2026-08-19

> 学员 8/19 13:15 补交周检（commit `b74114c`，单独 commit + push，符合 8/18 任务书「补完单独提交」要求）。Day 13 任务今日进行中，其批改并入次日流程。

## 结论：通过 ✅ ｜ 代码 3/3 实跑全过 ｜ 文字题 3/3 答到核心（2 处概念订正）

## 实跑证据（教练实跑，exit=0）

```
周检 Q1 过
周检 Q2 过
周检 Q3 过
EXIT_CODE=0
```

## 逐项验收

| 维度 | 判定 | 证据 |
|---|---|---|
| 能否运行 | ✅ | 三题自测全部通过，无运行错误 |
| 是否达标 | ✅ | q1 列表推导式 + 生成器表达式（惰性、`next()` 顺序对）；q2 三层嵌套闭包捕获 `times`、返回最后一次结果、执行次数断言过；q3 dataclass 字段/方法、`sorted` 不动原列表、内部协程 + `gather(*mov_list)` 保序 |
| 代码质量 | 小改进 | q2 `result_list` 收集再取 `[-1]` 绕路——循环里用 `result = func(...)` 覆盖即可；脚手架导入了 `functools` 但没用，建议 wrapper 上加 `@functools.wraps(func)`（保留函数名/文档，第 3 周 FastAPI 依赖注入用到）；q3 内层 `one()` 可简化为 `asyncio.sleep` 闭包但当前写法正确 |
| 是否抄袭 | ✅ 无 | q2 用「列表收集再取末位」的朴素写法、q3 注释「#有几层嵌套就要返回几层」「#lambda 将实例对象指向 m」都是自我解释痕迹，不是抄现成模式 |
| 文字题 | ✅ 通过 | 三题核心全对，2 处概念订正见下 |

## 文字题点评

1. **推导式 vs for vs 生成器（内存）**：答对关键——`[...]` 一次性全量进内存、`(...)` 惰性逐个产出、10 万元素 `[...]` 先爆。「生成器每次调用只运行一次，保留运行状态」表述略含糊（准确说：每次 `next()` 只算出一个元素、挂起时保留执行位置），方向正确不扣分。
2. **装饰器语法糖与三层嵌套**：`@timer` 等价于 `func = timer(func)` ✅；三层职责「外层收参数、中层收函数、内层收调用参数」✅。表述「第三层传入装饰的功能」有点绕但意思对。
3. **dict vs dataclass**：总结「临时数据用 dict、需要统一管理/计算时用类」非常到位。两处订正：
   - 「dict 键值查找通常快于类属性查找」——**不准确**。dataclass 实例属性也存在实例的 `__dict__` 里，属性访问本质上同样是字典查找，速度同量级，不存在「通常更快」。别把这条带进面试。
   - 「Student 类自动补全，一般拼错不了」——严格说拼错属性一样抛 `AttributeError`，IDE 补全只是减少手输错误，不是语言保证。你和 dict 拼错 key 的 `KeyError` 对比方向对，但表述要收敛。

## 三问（8/19 起每日三问；周检无新三问）

周检无三问。上述 2 处概念订正建议补进今日笔记「学习笔记」节（第 3 周面试前复习用）。

## 欠账

- 无。周检欠账已清。
- Day 12 笔记 2 处订正（`return_exceptions` 拼写 / `asyncio.run` 可多次调用）在 8/19 任务书状态核对里已要求学员改，未验证，并入 Day 13 批改。
