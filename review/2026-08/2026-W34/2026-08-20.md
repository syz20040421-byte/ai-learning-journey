# Day 14 批改 · 2026-08-20 · Pydantic：数据校验自动化

> 提交：`submissions/2026-08/2026-W34/2026-08-20/pydantic_practice.py`（commit `62f12f6`，学员自 push）
> 核验时间：2026-08-21（教练实跑）

## 结论：通过 ✅（streak 1→2）

## 实跑记录

```
$ ".venv/Scripts/python.exe" submissions/2026-08/2026-W34/2026-08-20/pydantic_practice.py
Day 14 全过
exit=0
```

8 组自测断言全部通过（合法构造 / 宽松转换 / 分数越界 / 好行容错 / 坏行定位 / 嵌套整体校验 / 缺字段整体拒收 / 及格率+空列表）。

## 验收标准逐条

| 项 | 结果 |
|---|---|
| 实跑 exit=0 输出「Day 14 全过」 | ✅ |
| Student/ClassRoom 两模型 + 4 函数齐全，自测块未动 | ✅ |
| `pyproject.toml` 仓库根存在，含 name + `dependencies=["pydantic>=2"]` | ✅（8/21 教练装第 3 周依赖时 uv 自动追加了 fastapi/httpx/uvicorn，学员侧无需处理） |
| `README.md` ≥10 行（简介+目录+运行示例） | ✅ 17 行，三个要素齐全 |
| `git ls-files | grep __pycache__` 无输出 | ✅ 0 条，.gitignore 生效 |
| 笔记非空 + 三问全答；Day 12 笔记 2 处订正已改 | ✅ 见下 |
| commit + push | ✅ `62f12f6` 在 origin/main |

## 代码质量

- `safe_score` 的 `try/except/else` 结构干净，`loc[0]` 取字段名、错误信息拼装符合要求
- `load_class` 一行 `ClassRoom(students=rows)`，没手写循环逐条转——理解了「嵌套模型递归校验」是 Pydantic 白送的
- 小尾巴（不扣分，下次注意）：
  - `make_student` 里 `stu_return` 中间变量多余，`return Student(...)` 即可
  - `pass_rate` 手动 for 循环计数，可以用推导式 + `sum()` 一行：数 `score >= 60` 的个数再除总数（Day 15 不考，知道有更短的写法就行）
  - 变量名 `a` 太随意，统计类变量叫 `passed` / `n_pass` 读起来更清楚

## 三问核验

1. **（async 真实网络追问）** 答「不会」「不成立，网络实时波动的」「改法：限定时间在同一网络下反复串行/并发对比完成度」——**方向对，需订正一处**：真实网络下**并发通常还是比串行快**（IO 等待时间被重叠，这正是异步的意义），但**单次测量的耗时抖动大**，所以断言会 flaky。你的改法（多次测、比完成度/中位数）是对的，Day 15 预习里会再讲。核心结论记住：**并发快是规律，单次断言不可靠是测量问题**——两个不矛盾。
2. **（宽松转换）** 布尔 `True` → 1（bool 是 int 子类）✅ 全对，还点出了「毫无异常」——这正是坑的隐蔽处。
3. **（逐条 vs 整体）** 批量导入选 `safe_score`、重要数据整体校验选 `load_class`、产品要坏行跳过选逐条、FastAPI 默认整体校验（422）✅ 全对。最后一句答出了下周要用的事实。

## 笔记核验

- 学习笔记 5 条，卡点真实且有价值：**「直接 `Student(name=row["name"],...)` 缺字段会抛 KeyError 而不是 ValidationError，改成 `Student(**row)` 解包让 Pydantic 校验所有字段」**——这是今天最容易踩的坑之一，你踩了、查了、改对了，这比一遍过更有含金量
- Day 12 笔记 2 处订正：`return_exceptions=True` 拼写已改 ✅；`asyncio.run` 表述已改（可多次调用、不能嵌套循环调用）✅
- 三问都答了，Q2/Q3 满分

## 状态更新

- streak：1→2
- skills_assessment：新增 `pydantic` 维度不在此表（第 3 周 FastAPI 会反复用，届时并入评估）；`python_datastructures` 维持
- weak_points：无新增
- history 追加：`{date: 2026-08-20, week: 2, topic: "Day 14（Pydantic）", submitted: true, passed: true, note: "实跑 exit=0 一遍过..."}`

## 遗留

- Day 12 周检补交的 q2 `result_list` 简化建议（8/19 批改提过）：学员未回复是否采纳，不追
- README 已建 ✅，第 2 周验收「仓库开张」闭环。第 3 周开始滚 HTTP/FastAPI/SQL
