# 2026-08-07 · Day 5 批改报告（第一轮）

> 批改对象：`submissions/2026-08/2026-W32/2026-08-07/`（04_file_io.py + score.txt）+ `notes/2026-08/2026-W32/2026-08-07-file-io.md`
> 方式：实跑 `04_file_io.py`(python 3.11),结果如下。

## 逐项判定

| 项目 | 结果 | 运行输出 | 判定 |
|---|---|---|---|
| 04_file_io.py 运行 | ✅ exit=0 | `文件不存在` ×2 + `{}` | **能跑** |
| 正常文件解析 | ✅ | `{"张三":100,"里斯":90,"英超":85}` | 达标 |
| 文件不存在返回 `{}` | ✅ 断言验证 | 打印提示 + 无 traceback | 达标 |
| `with open(...)` + `encoding="utf-8"` | ✅ | — | 达标 |
| **`scores.txt` 数据文件** | ❌ **文件名是 `score.txt`(少 s)**,且**只有 3 行有效数据,无空行,无坏行** | — | **不达标,必须补** |
| **坏行跳过提示(含行号)** | ⚠️ 代码逻辑在,但**数据文件里没有坏行,运行时从未触发过** | 无任何「第 N 行跳过」输出 | **未被真实验证** |
| **周检 4 题** | ❌ **全空**(notes 文件只有题目模板) | — | **不达标,必须补** |
| **笔记 3 题** | ❌ **全空** | — | **不达标,必须补** |
| **英语摘录(5 词+1 句+3 句总结)** | ❌ **全空** | — | **不达标,必须补** |
| git add + commit + push | ❌ 未提交(3 个文件还在工作区) | — | **不达标,必须补** |

## 亮点(点名)

- `parse_score_line` 里 `parts[0].strip()` / `int(parts[1].strip())`——比昨天多了去空格,处理 `"张三, 100"` 这类带空格行也能过,讲究。
- try/except/else 结构用上了:解析成功才写进 dict,else 分支用法正确,和昨天 refactor 的方向一致。
- 文件不存在分支处理正确:打印提示、返回 `{}`、无 traceback,且 `file_line` 为空列表时循环自然跳过,逻辑干净。

## 问题(必须补)

1. **数据文件不合格**:任务要求 `scores.txt` 至少 4 行有效数据、**1 行空行、2 行坏行**(一行无逗号、一行分数非整数)。你只放了 3 行全好的,坏行逻辑根本没被数据触发过——验收标准里「坏行打印跳过提示」这条形同虚设。文件名也从 `score.txt` 改成 `scores.txt`。
2. **notes/2026-08/2026-W32/2026-08-07-file-io.md 是空模板**:英语词汇、摘抄、心得、周检 4 题、笔记 3 题、今日一问,全部只复制了题目没写答案。
3. **没 commit**:任务验收标准最后一条是 `git add + commit + push`,3 个文件还在工作区。

## 判定

**代码主体完成、思路正确,但数据文件不达标(坏行逻辑未被验证)+ 笔记/周检/英语全空 + 未提交 → 整体不通过。** 补齐后复核。

## 打卡

- 有实质提交(代码在工作区)→ 不记缺勤;通过与否待补齐后确认,streak 暂不动

---

# 最终复核(2026-08-08)

> 学员补齐三处后提交(commit 0a63128 已推送),复核如下。

| 待补项 | 修复观察 | 判定 |
|---|---|---|
| 数据文件不合格 | `scores.txt` 现含 4 行有效(张三/里斯/英超/法甲)+ 1 空行 + 2 坏行(西甲无逗号、德甲非整数);实跑打印「第2行跳过:空行」「第5行跳过:缺少逗号」「第6行跳过:分数不是整数」 | ✅ 已补 |
| 笔记/周检/英语全空 | `notes/2026-08/2026-W32/2026-08-07-file-io.md` 补全:英语摘录 15 词+1 句+3 段心得、周检 4 题全答、笔记 3 题、今日一问(答出 `__enter__`/`__exit__`) | ✅ 已补 |
| 未提交 | commit 0a63128 已推送,含 3 个文件 | ✅ 完成 |

**最终判定:Day 5 整体通过。** streak current=5,longest=5,`file_io` 评估 0→2,弱项「文件IO」移除。今日一问答出 `__enter__`/`__exit__` 机制,超出本周范围,值得点名。

**下次任务注意**:数据文件质量是验收硬标准——测试数据必须包含任务要求的坏行组合,否则代码路径覆盖不到;周检/笔记不能只贴题目。
