# Day 7 批改 · 2026-08-11（批 8/10 提交）

## 结论：未完成 —— 代码没写，笔记是空模板

提交存在（`submissions/2026-08-10/06_integration.py`，commit 833196f / 98e267a 已 push），但主体交付缺失。不是「做完但有错」，是「没开始做」。

## 实跑结果

- `06_integration.py` → exit=0，**但没有任何输出**。三个函数（`load_cities` / `fetch_weather` / `save_results`）全部是 `pass` 桩，主流程是空的，`assert/自测` 段也是空的。exit=0 只证明「空文件跑完了」。
- `submissions/2026-08-10/` 下没有 `cities.csv`、没有 `weather.csv` —— 没有任何真实运行痕迹。
- `notes/2026-08-10-integration.md`（23:38 创建，未提交）是**空模板**：只贴了题目原文，词汇、摘抄、心得、4 个笔记问题、今日一问全部没答。

## 完成的部分（要肯定）

- **Day 6 收尾全部闭环**：心得 3 句、卡点复盘、问题 2 末句已改成「同时指向这个列表」（共享语义对了），都在 commit 98e267a 里，已 push。昨天 review 里列的待办，你全清了。
- 21:22 交脚手架、21:37 补 Day 6 —— 人在动，不是没来。

## 为什么没写？（判断卡点，不指责）

最可能的卡点：`python 06_integration.py` 报 `ModuleNotFoundError: No module named 'requests'` —— 你的 `python` 命令指向 hermes 环境，不是仓库的 `.venv`。昨晚 23:35 已把解决办法写进任务书第 0 条：

```bash
source .venv/Scripts/activate    # 激活后再跑 python 06_integration.py
# 或直接
.venv/Scripts/python.exe 06_integration.py
```

环境我今早实测过：`.venv` 里 requests 2.33.0 可用，open-meteo 返回 200、29.1℃。链路是通的。

如果卡的不是环境，是「不知道 requests 怎么用」—— 预习清单第 1–2 条（`requests.get(url, timeout=10)`、`.status_code`、`.json()`）就是答案，对着写。
如果是「写不出来」—— 把卡住的那一行写进笔记，明天批改我针对那一行讲。**交 pass 桩我无法判断你卡在哪，等于白交。** 下次卡住先写「我卡在：___」，再提交。

## 处置

- Day 7 记为**未完成**，不记缺勤（你交了东西、也 push 了，是没做完不是没来）。streak 停在 6，不加不减。
- 今天（Day 8）**续做综合验收 Part B**——这是第 1 周唯一的交付物（「一个自己 debug 通的脚本」，ROADMAP 第 1 周验收原文）。不补完不进第 2 周。任务书在 `daily/2026-08-11.md`。
- 补完并验收通过后，明天起进入第 2 周（Python 进阶 + Git branch）。
